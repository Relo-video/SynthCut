{
  "name": "@aive/mcp",
  "version": "0.1.2",
  "description": "MCP server for the AI-Native Video Editor. Exposes the editor's tools to any MCP-compatible AI client (e.g. Claude Desktop) and forwards them to the running editor core.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "aive-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@aive/core": "*",
    "@modelcontextprotocol/sdk": "^1.13.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
